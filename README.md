# A watch built in rust.

At first it will be without timezone support.
Built it without chrono. So sticking to that for now.

## TODO:
- Customize the size of the window in gtlk.
- Adding timezone support later
- Add a dial showing the time instead of string
- Change to support milliseconds to have the dial continously moving.



